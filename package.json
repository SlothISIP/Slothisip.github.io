{
  "name": "slothisip-portfolio",
  "version": "2.0.0",
  "description": "Ju O Kim - AI Research Engineer portfolio (Eleventy build)",
  "private": true,
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "rimraf _site .cache",
    "build": "eleventy",
    "serve": "eleventy --serve --quiet",
    "start": "eleventy --serve",
    "debug": "cross-env DEBUG=Eleventy* eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^5.0.0",
    "cross-env": "^7.0.3",
    "js-yaml": "^4.1.0",
    "rimraf": "^6.0.1"
  }
}
